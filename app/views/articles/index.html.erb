<ul class="list-group">
  <% @articles.each do |article| %>
    <div class="text-left" style="margin: 0 33%">
      <h2><%= link_to "#{article.title}", article_path(article), class: "mb-3" %></h2>
      <p><%= article.content.first(75) %>...</p>
    </div>
  <% end %>
</ul>
